name: flutter_app
description: A new Flutter application.
publish_to: 'none' 
version: 1.0.7+7

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1
  # flutter_web_plugins:
  #   sdk: flutter
  # # Removes /#/ from URL in Flutter Web
  # url_strategy: ^0.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_screenutil: 3.2.0

flutter:
  uses-material-design: true

  assets:
    - assets/
